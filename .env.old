# Application
APP_NAME=ITQuty2
APP_ENV=local
APP_KEY=base64:Oyc9o3gRmXTEuovuH45g94FQFkhsPrX9U+qbX1Hr3+c=
APP_DEBUG=true
APP_URL=http://192.168.1.87
APP_TIMEZONE=Asia/Jakarta

# Logging
LOG_CHANNEL=errorlog
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3308
DB_DATABASE=itquty
DB_USERNAME=itquty_user
DB_PASSWORD=itquty123

# Drivers & Storage
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=database
SESSION_LIFETIME=120

# Redis (if needed)
REDIS_HOST=192.168.1.87
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail
MAIL_MAILER=noverifysmtp
MAIL_HOST=mx.quty.co.id
MAIL_PORT=465
MAIL_USERNAME=ims.it@quty.co.id
MAIL_PASSWORD=Quty123$
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS=ims.it@quty.co.id
MAIL_FROM_NAME=IMSQuty

# AWS (if using S3)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher (if using)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Vite (for asset building)
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Custom - Slack Integration
SLACK_ENABLED=false
SLACK_WEBHOOK=
SLACK_CHANNEL=#general
SLACK_BOT_NAME="Quty Asset Bot"