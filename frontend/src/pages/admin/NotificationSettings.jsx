import Typography from '@mui/material/Typography';
import Box from '@mui/material/Box';

export default function AdminNotificationSettings() {
  return (
    <Box>
      <Typography variant="h5">Notification Settings (Admin)</Typography>
      <Typography sx={{ mt: 2 }}>Placeholder for admin notification settings.</Typography>
    </Box>
  );
}
