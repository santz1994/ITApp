import Typography from '@mui/material/Typography';
import Box from '@mui/material/Box';

export default function LcdSettings() {
  return (
    <Box>
      <Typography variant="h5">LCD Settings</Typography>
      <Typography sx={{ mt: 2 }}>Placeholder for LCD settings.</Typography>
    </Box>
  );
}
