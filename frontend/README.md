# Frontend (React)

This folder contains the React frontend for ITApp.

Quick start:

```bash
cd frontend
npm install
npm start
```

The app proxies API requests to the Laravel backend at `http://localhost:8000`.
